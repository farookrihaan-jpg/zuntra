# During development the Vite proxy forwards /api → http://localhost:5000
# so no VITE_API_URL is needed locally.
#
# For production builds pointed at a deployed backend:
VITE_API_URL=https://your-backend.railway.app
